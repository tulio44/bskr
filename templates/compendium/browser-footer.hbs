<div class="flexrow">
    {{~#if displaySelection}}
    <div class="count {{ invalid }}">
        {{{ localize "BSKR.CompendiumBrowser.Selection.Label" summary=summary }}}
    </div>
    <button type="submit">
        {{ localize "BSKR.CompendiumBrowser.Selection.Select" }}
    </button>
    {{/if~}}
</div>
