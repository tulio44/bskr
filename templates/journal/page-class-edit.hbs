<form class="{{ cssClass }} flexcol drop-target" autocomplete="off">
    {{> journalEntryPageHeader }}

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.item.label" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#if document.system.item}}
                <li class="item flexrow" data-item-uuid="{{ document.system.item }}" data-item-type="linked">
                    <div class="item-name">{{{ bskr-linkForUuid document.system.item }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control item-delete" data-tooltip="BSKR.ItemDelete"
                           aria-label="{{ localize 'BSKR.ItemDelete' }}">
                            <i class="fa-solid fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.BSKR.Class.ItemHint" }}</li>
                {{/if}}
            </ol>
        </div>
    </div>

    {{ formField systemFields.style value=system.style options=styleOptions }}

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.value.label" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.value">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.BSKR.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.value.hint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalHitPoints.label" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalHitPoints">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.BSKR.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalHitPoints.hint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalTraits.label" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalTraits">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.BSKR.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalTraits.hint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalEquipment.label" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.additionalEquipment">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.BSKR.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.additionalEquipment.hint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.subclassHeader.label" }}</label>
        <div class="form-fields">
            <input type="text" name="system.subclassHeader" value="{{ system.subclassHeader }}">
        </div>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.subclass.label" }}</label>
        <div class="form-fields">
            <button class="launch-text-editor" data-target="system.description.subclass">
                <i class="fas fa-edit" inert></i> {{ localize "JOURNALENTRYPAGE.BSKR.EditDescription" }}
            </button>
        </div>
        <p class="hint">{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.description.subclass.hint" }}</p>
    </div>

    <div class="form-group">
        <label>{{ localize "JOURNALENTRYPAGE.BSKR.Class.FIELDS.subclassItems.label" }}</label>
        <div class="form-fields">
            <ol class="items-list">
                {{#each subclasses}}
                <li class="item flexrow" data-item-uuid="{{this.document.uuid}}" data-item-type="subclass">
                    <div class="item-name">{{{ bskr-linkForUuid this.document.uuid }}}</div>
                    <div class="item-controls flexrow">
                        <a class="item-control item-delete" data-tooltip="BSKR.ItemDelete"
                           aria-label="{{ localize 'BSKR.ItemDelete' }}">
                            <i class="fas fa-trash" inert></i>
                        </a>
                    </div>
                </li>
                {{else}}
                <li class="item flexrow">{{ localize "JOURNALENTRYPAGE.BSKR.Class.SubclassHint" }}</li>
                {{/each}}
            </ol>
        </div>
    </div>
</form>
