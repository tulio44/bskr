<section class="flexcol">
    <div class="note info">
        {{ localize (ifThen isGroup "BSKR.REST.Long.Hint.Group" "BSKR.REST.Long.Hint.Normal") }}
    </div>

    {{#if fields.length}}
    <fieldset>
        <legend>{{ localize "BSKR.REST.Configuration" }}</legend>
        {{#each fields}}
        {{ formField field name=name value=value input=input options=options rootId=../partId }}
        {{/each}}
    </fieldset>
    {{/if}}
</section>
