<section>
    {{~#if hasConcentration}}
    <fieldset>
        <legend>{{ localize "BSKR.Concentration" }}</legend>
        {{#each fields}}
            {{ formField field name=name value=value options=options input=input }}
        {{/each}}
    </fieldset>
    {{> "bskr.activity-usage-notes" }}
    {{/if~}}
</section>
