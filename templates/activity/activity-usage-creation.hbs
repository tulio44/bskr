<section>
    {{~#if hasCreation}}
    <fieldset>
        <legend>{{ localize "BSKR.USAGE.SECTION.Creation" }}</legend>
        {{#if template}}
            {{ formField template.field name=template.name value=template.value input=inputs.createCheckboxInput }}
        {{/if}}
    </fieldset>
    {{> "bskr.activity-usage-notes" }}
    {{/if~}}
</section>
