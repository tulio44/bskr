<div class="bskr2 chat-card request-card">
    <div class="card-buttons">
        {{#each buttons}}
        <button {{ bskr-dataset dataset }}>
            <span class="visible-dc">{{{ buttonLabel }}}</span>
            <span class="hidden-dc">{{{ hiddenLabel }}}</span>
        </button>
        {{/each}}
    </div>
</div>
