<div class="bskr2 chat-card bastion-card">

    {{!-- Orders --}}
    {{#if orders.length}}
    <ul class="unlist order-summary">
        {{#each orders}}
        <li>{{{ localize "BSKR.Bastion.Summary.Order" link=contentLink order=order }}}</li>
        {{/each}}
    </ul>
    {{else}}
    <p class="order-description">{{ localize "BSKR.Bastion.Summary.NoOrders" }}</p>
    {{/if}}

    {{!-- Items --}}
    {{#if items.length}}
    <ul class="unlist item-summary">
        {{#each items}}
        <li class="item-tooltip" data-uuid="{{ uuid }}" data-quantity="{{ quantity }}" data-action="viewItem"
            data-tooltip="<section class='loading' data-uuid='{{ uuid }}'><i class='fas fa-spinner fa-spin-pulse'></i></section>">
            <img src="{{ img }}" alt="{{ name }}">
        </li>
        {{/each}}
    </ul>
    {{/if}}

    {{!-- Supplements --}}
    {{#each supplements}}
    <p class="supplement">{{{ this }}}</p>
    {{/each}}

    {{!-- Buttons --}}
    {{#if buttons.length}}
    <div class="card-buttons">
        {{#each buttons}}
        <button type="button" {{ bskr-dataset dataset }}>
            {{{ icon }}} <span>{{{ label }}}</span>
        </button>
        {{/each}}
    </div>
    {{/if}}

</div>
